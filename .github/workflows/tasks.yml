name: CI
on:
  push:
jobs:
  Test:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Install Dependencies
        run: make install
      - name: Test
        run: make test
